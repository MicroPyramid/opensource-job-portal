# Recruiter UI Environment Variables

# API Base URL (Django backend)
PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Site URL (Frontend URL for OAuth callbacks)
PUBLIC_SITE_URL=http://localhost:5174

# Optional: Google OAuth Client ID (if different from job seeker UI)
# PUBLIC_GOOGLE_CLIENT_ID=your-client-id-here
